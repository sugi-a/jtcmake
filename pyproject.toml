[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.black]
include = '(src|tests)/.+\.pyi?$'
line-length = 79

[tool.pyright]
include = ['tmp_typecheck']
pythonVersion = "3.10"
